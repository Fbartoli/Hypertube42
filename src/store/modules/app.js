const state = {
  appName: "Hypertube",
  Loading: false,
  langs: ["fr", "en"],
  userInfo: {}
};

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
