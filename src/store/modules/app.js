const state = {
  appName: "Hypertube",
  Loading: false,
  langs: ["fr", "en"],
  userInfo: {
    email: "",
    username: "",
    token: "",
    auth: false
  }
};

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
